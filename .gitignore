# Rust
/target
**/target

# Node / Tauri frontend
node_modules
dist
.vite
/src-tauri/gen
package-lock.json.bak

# Binarios de rclone descargados (~80 MB cada uno). Se obtienen con
# `python3 scripts/descargar-rclone.py`, no se versionan.
/src-tauri/binaries/
/src-tauri/resources/rclone*

# --- SECRETOS Y DATOS DE CLIENTES ---
# El repositorio es PUBLICO. Nada de esto puede entrar nunca.
*.env
.env*
config.local.json
probe-report-*.json
probe-report-*.txt
credenciales*
